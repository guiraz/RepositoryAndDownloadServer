extends layout

mixin link(href, value)
    a(href=href)= value
block content
    if !user
        p Welcome! Please <a href="/login">log in</a>.
    else
        p Hello, #{user.username}. View your <a href="/profile">profile</a>.